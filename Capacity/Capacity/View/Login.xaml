<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             NavigationPage.HasBackButton="True"
             Title="Inicio de sesión"
             xmlns:viewmodel="clr-namespace:Capacity.ViewModel"
             x:Class="Capacity.View.Login">
    <ContentPage.BindingContext>
        <viewmodel:LoginViewModel></viewmodel:LoginViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Padding="10,20,10,40">
            <Label Text="Usuario"></Label>
            <Entry BindingContext="{Binding Email,Mode=TwoWay}"></Entry>
            <Label Text="Contraseña"></Label>
            <Entry BindingContext="{Binding Password, Mode=TwoWay}" IsPassword="True"></Entry>
            <Button Text="Iniciar sesión" BindingContext="{Binding LoginCommand}" CornerRadius="10" 
                    TextColor="White" BackgroundColor="#ff4d4d" VerticalOptions="End" HorizontalOptions="FillAndExpand"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>